{
  "name": "agentos-workspace",
  "version": "0.4.10",
  "description": "Force selected coding tasks through AgentOS MCP sessions before any file edits.",
  "author": {
    "name": "AgentOS"
  },
  "repository": "https://github.com/shinyeonjun/AgentOS",
  "license": "MIT",
  "keywords": ["agentos", "codex", "sandbox", "workspace", "approval"],
  "skills": "./skills/",
  "mcpServers": "./.mcp.json",
  "interface": {
    "displayName": "AgentOS Workspace",
    "shortDescription": "Route coding work through AgentOS sessions.",
      "longDescription": "AgentOS Workspace routes Codex coding work through copied AgentOS session workspaces, review packages, and approval-gated sync instead of editing host projects directly. The default UX is sync-only approval: doctor, session creation, session work, tests, and review can run without extra prompts because they do not mutate the original project; syncing back to the original project remains the explicit human approval boundary. When selected, Codex must call the bundled AgentOS MCP server before any file edit and run the setup workflow if AgentOS tools are unavailable.",
    "developerName": "AgentOS",
    "category": "Productivity",
    "capabilities": ["Write", "Code"],
    "defaultPrompt": [
      "Use AgentOS MCP for this task. Before any file edit, call agentos doctor. If tools are unavailable, offer to run AgentOS setup.",
      "Use the normal approval policy by default: create/reuse sessions, run commands, test, and review without asking because the original workspace is not changed.",
      "Do not edit the current/original workspace directly. Work only inside the returned workspace_path. If Docker or the AgentOS image is not ready, call prepare_environment before Docker sandbox work.",
      "Produce and verify a review package, run sync_preflight to show planned paths and blockers, then wait for explicit human approval before approve_scope or sync_approved changes the original project."
    ],
    "brandColor": "#2563EB"
  }
}
